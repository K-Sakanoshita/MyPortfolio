export const metadata = {
  title: "坂ノ下 勝幸 | Open Data, Open Source & Community"
};

export default function RootLayout({ children }) {
  return (
    <html lang="ja">
      <body>{children}</body>
    </html>
  );
}
